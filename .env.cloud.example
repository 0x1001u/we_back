# 微信云托管环境变量配置示例
# 请在微信云托管控制台的"环境变量"中配置这些变量

# 应用基础配置
NODE_ENV=production
PORT=80
USE_CLOUD=true

# JWT密钥配置（必须修改）
JWT_SECRET=your-super-secret-jwt-key-change-me-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=120
JWT_REFRESH_EXPIRE_MINUTES=10080

# 微信小程序配置（从小程序后台获取）
WECHAT_APP_ID=your_wechat_app_id
WECHAT_APP_SECRET=your_wechat_app_secret

# 微信支付配置（可选，如需支付功能）
WECHAT_MCH_ID=your_mch_id
WECHAT_MCH_KEY=your_mch_key
WECHAT_SUB_MCH_ID=your_sub_mch_id
WECHAT_SUB_MCH_KEY=your_sub_mch_key

# 云托管环境ID
CLOUD_ENV_ID=your_cloud_env_id

# 数据库配置（云托管自动注入）
# 注意：在微信云托管中，数据库配置通常由平台自动注入
# 如果使用云数据库，这些变量会自动设置
DB_HOST=your_db_host
DB_PORT=3306
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# 其他配置
DISABLE_WECHAT_SSL_VALIDATION=true
UPLOAD_DIR=uploads
MAX_UPLOAD_SIZE=10485760
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# 微信云托管部署说明：
# 1. 在微信云托管控制台创建服务
# 2. 选择从代码仓库部署或上传代码包
# 3. 在"环境变量"页面配置上述变量
# 4. 确保数据库服务已创建并正确配置连接信息
# 5. 部署后访问 /health 接口检查服务状态